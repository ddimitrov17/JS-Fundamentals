function requiredReading(book,pagesInHour,requiredDays) {
    let timeToRead=book/pagesInHour;
    let hoursPerDay=timeToRead/requiredDays;
    console.log(hoursPerDay);
}

requiredReading(212,

    20 ,
    
    2)